<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    tools:context=".PaymentPage">

    <!-- Header section -->
    <RelativeLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <!-- Logo (ImageView) -->
        <ImageView
            android:id="@+id/logo"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_alignParentStart="true"
            android:src="@drawable/ologo"
            android:contentDescription="Shinchi Logo"
            />

        <TextView
            android:id="@+id/payment_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:fontFamily="cursive"
            android:text="Payment"
            android:textSize="60sp"
            android:layout_marginRight="30dp"
            android:textColor="#ffae00"
            android:textStyle="bold" />

    </RelativeLayout>

    <View
        android:id="@+id/header_separator"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/header_layout"
        android:background="@android:color/darker_gray" />

    <!-- Total amount section -->
    <LinearLayout
        android:id="@+id/total_layout"
        android:layout_width="411dp"
        android:layout_height="154dp"
        android:layout_below="@id/header_separator"
        android:layout_marginTop="32dp"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Total:"
            android:layout_marginTop="30dp"
            android:textColor="#ffae00"
            android:textSize="70sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/total_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="₹0.00"
            android:layout_marginTop="30dp"
            android:textColor="@android:color/black"
            android:textSize="70sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Bottom separator line for the address -->

    <!-- "Pay Now" button -->

    <View
        android:id="@+id/address_separator_top"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/total_layout"
        android:layout_marginTop="30dp"
        android:background="@android:color/darker_gray" />

    <LinearLayout
        android:id="@+id/address_layout"
        android:layout_width="wrap_content"
        android:layout_height="203dp"
        android:layout_below="@id/address_separator_top"
        android:layout_marginTop="59dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/address_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:fontFamily="sans-serif-medium"
            android:text="Address:"
            android:textColor="#ffae00"
            android:textSize="30sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/address_details"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/Address"
            android:textColor="@android:color/black"
            android:textSize="20sp" />
    </LinearLayout>

    <View
        android:id="@+id/address_separator_bottom"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@id/address_layout"
        android:layout_marginTop="59dp"
        android:background="@android:color/darker_gray" />

    <Button
        android:id="@+id/pay_now_button"
        android:layout_width="313dp"
        android:layout_height="153dp"
        android:layout_below="@+id/address_separator_bottom"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="82dp"
        android:layout_marginTop="41dp"
        android:layout_marginEnd="77dp"
        android:layout_marginBottom="147dp"
        android:background="@drawable/btn_filled"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:text="Pay Now"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:backgroundTint="@null" />

</RelativeLayout>
